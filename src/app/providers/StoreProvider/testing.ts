//SPECAIL public api for test env
//use only inside .test/.spec files and shared/lib/test dir
//should create the appropriate rule in own eslint plugin
export { createReduxStore } from './model/store';
